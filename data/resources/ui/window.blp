using Gtk 4.0;
using Adw 1;

template $Window: Adw.ApplicationWindow {
    default-width: 600;
    default-height: 300;
    title: _("Hello, Blueprint!");

    Adw.HeaderBar {}

    Label label {
        label: bind template.main-text no-sync-create;
    }
}
