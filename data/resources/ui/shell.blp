using Gtk 4.0;
using Astal 4.0;
using Adw 1;

template $Shell: Astal.Window {
    default-width: 600;
    default-height: 300;
    title: _("Hello, Blueprint!");

    Label label {
        label: bind template.main-text no-sync-create;
    }
}
