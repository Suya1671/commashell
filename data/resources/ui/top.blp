using Gtk 4.0;
using Astal 4.0;

template $Top: Astal.Window {
    default-height: 60;
    title: _("Hello, Blueprint!");
    exclusivity: exclusive;
    anchor: top | left | right;

    Label label {
        label: bind template.main-text no-sync-create;
    }
}
